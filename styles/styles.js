import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexGrow: 1,
    },
    camera: {
        flex: 1,
    },
    emotionText: {
        position: 'absolute',
        fontSize: 25,
        fontWeight: 'bold',
        marginTop: -35,
        textAlign: 'center',
        color: "red",
      },
});
